package main

import (
	"fmt"
	"math"
)

func main() {
	fmt.Print("Enter number :")
	var input int
	fmt.Scan(&input)

	loopCount := int(math.Abs(float64(input)))

	for i := 0; i < loopCount; i++ {
		for j := 0; j < loopCount; j++ {
			fmt.Print("#")
		}
		fmt.Println()
	}
}
